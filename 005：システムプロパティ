* 問題
    * proxy.example.com:80を使用するように指定
    * #001のプログラムを実行するjavaコマンドを実行
    * *.noproxy.example.comへのアクセスはプロキシサーバを利用せず直接接続
* 解答
    * $ java -Dhttp.proxyHost=proxy.example.com -Dhttp.proxyPort=80 -Dhttp.http.nonProxyHosts=*.noproxy.example.com Task01
* 補足
    * http.proxyHostオプションで、プロキシサーバのホスト名を指定
    * http.proxyPortオプションで、ポート番号を指定
    * http.nonProxyHostsオプションで、プロキシを省略して直接接続するホストを指定
