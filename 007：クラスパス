* 問題
    * classファイルが~/java/classesディレクトリにある。
    * 動作させるのに必要なライブラリ(jarファイル)が~/java/libディレクトリに複数存在する場合のJavaプログラムの実行コマンド。
* 解答
    * $ java -cp ~/java/classes:~/java/lib/* Answer007
* 補足
    * -cpオプション（-classpathオプション) -> クラスファイルを起動
    * PATHが複数存在する場合　-> コロン（：）で区切る
